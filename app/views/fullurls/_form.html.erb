<%= form_for(fullurl) do |f| %>
  <% if fullurl.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fullurl.errors.count, "error") %> prohibited this fullurl from being saved:</h2>

      <ul>
      <% fullurl.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url_string %>
    <%= f.text_field :url_string %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
